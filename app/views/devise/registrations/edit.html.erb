<div class="row">
  <div class="col-md-4 col-md-offset-4">

    <h2>Настройки</h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>
      <div class="form-group">
        <%= f.email_field :email, autofocus: true, placeholder: "Поща", class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.text_field :name, autofocus: true, placeholder: "Име", class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.password_field :password, autocomplete: "off", placeholder: "Нова парола", class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: "Повтори паролата", class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.file_field :avatar %>
      </div>

      <div class="form-group">
        <%= f.text_area :bio, autocomplete: "off", placeholder: "Ако искате напишете нещо за себе си.", class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.check_box :subscribed %>
        <%= f.label :subscribed, "Абониран(а) за нашият бюлетин?" %>
      </div>

      <hr />
      <div class="form-group">
        <p class="text-muted"><i>За да промениш настройките, трябва да въведеш сегашната си парола.</i></p>
        <%= f.password_field :current_password, autocomplete: "off", placeholder: "Сегашна парола", class: "form-control" %>
      </div>

      <%= f.submit "Промени!", class: "btn btn-success" %>
      <%= link_to "Върни ме.", :back, class: "btn btn-default" %>

    <% end %>

    <hr />

  <h3>Опасна зона!</h3>
  <p>Не искам повече да съм регистриран в този сайт.</p>
  <p><%= button_to registration_path(resource_name), data: { confirm: "Абе, сигурен/сигурна ли си?" }, method: :delete, class: "btn btn-danger btn-lg" do %>
    <i class="fa fa-warning fa-lg"></i> Изтрий профила!
  <% end %></p>

  </div>
</div>
